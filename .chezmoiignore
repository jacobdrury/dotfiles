README.md
dot_config/zed/prompts/

# Repo-local editor settings (not installed into $HOME)
.zed/**

# Repo-local agent skills (not installed into $HOME)
.agents/**

# installed to /etc via run_onchange script (not into $HOME)
etc/

# gh — auth state (may contain oauth tokens)
.config/gh/hosts.yml

# nvim — generated / local / ephemeral
.config/nvim/lazy-lock.json
.config/nvim/doc/tags
.config/nvim/spell/**
.config/nvim/.luarc.json
.config/nvim/test.sh
.config/nvim/.DS_Store
