eval "$(/opt/homebrew/bin/brew shellenv zsh)"

# proto
export PROTO_HOME="$HOME/.proto"

typeset -U path PATH
path=(
  /opt/homebrew/opt/curl/bin
  "$PROTO_HOME/shims"
  "$PROTO_HOME/bin"
  "$HOME/.cargo/bin"
  "$HOME/go/bin"
  "$HOME/.bun/bin"
  "${path[@]}"
)
export PATH
